return {
  {
    "ThePrimeagen/vim-be-good",
    -- optionally control when it loads or any config
    cmd = "VimBeGood", -- maybe load when you run the command
    -- you could set lazy=false if you want it loaded on startup
    lazy = true,
    -- you can set options if needed
    -- opts = {},
  },
}
